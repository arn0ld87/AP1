# Publishable/Anon-Key: Browser-geeignet — Sicherheit kommt aus den
# RLS-Policies, nicht aus der Geheimhaltung dieses Keys.
VITE_SUPABASE_URL=https://DEIN-PROJEKT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=DEIN-PUBLISHABLE-KEY

# NIEMALS hier eintragen (nur serverseitig, z. B. Edge-Function-Secret):
# - SUPABASE_SERVICE_ROLE_KEY (umgeht RLS)
# - AWS_BEDROCK_API_KEY (Bedrock-Modellzugriff)
