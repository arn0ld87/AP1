# Werte für den Container-Build auf dem armserver (docker compose).
# Publishable-Key = Browser-geeignet; Sicherheit über RLS.
VITE_SUPABASE_URL=https://supabase.alexle135.de
VITE_SUPABASE_PUBLISHABLE_KEY=DEIN-PUBLISHABLE-KEY

# NIEMALS in diese Datei: SUPABASE_SERVICE_ROLE_KEY, AWS_BEDROCK_API_KEY —
# beide ausschließlich als Supabase-Edge-Function-Secrets serverseitig.
